<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Missão - AEGIS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="fas fa-shield-halved"></i> AEGIS</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/herois">Heróis</a></li>
                    <li class="nav-item"><a class="nav-link" href="/viloes">Vilões</a></li>
                    <li class="nav-item"><a class="nav-link" href="/poderes">Poderes</a></li>
                    <li class="nav-item"><a class="nav-link" href="/missoes">Missões</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <% if (success) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>
        <% if (error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>
        <h2 class="mb-4"><i class="fas fa-edit text-success"></i> Editar Missão</h2>

        <div class="card shadow-sm mb-3">
            <div class="card-body">
                <form action="/missoes/editar/<%= missao.IdMissao %>" method="POST" class="row g-3">
                    <div class="col-md-4">
                        <label class="form-label">Nome da Operação</label>
                        <input type="text" name="nome" class="form-control" value="<%= missao.NomeMissao %>" required>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">Data de Solicitação</label>
                        <input type="date" name="data" class="form-control" value="<%= missao.DataSolicitacao ? missao.DataSolicitacao.toISOString().slice(0,10) : '' %>">
                    </div>
                    <div class="col-md-2">
                        <label class="form-label">Nível de Ameaça</label>
                        <select name="nivel" class="form-select">
                            <option value="Baixo" <%= missao.NivelAmeaca === 'Baixo' ? 'selected' : '' %>>Baixo</option>
                            <option value="Médio" <%= missao.NivelAmeaca === 'Médio' ? 'selected' : '' %>>Médio</option>
                            <option value="Alto" <%= missao.NivelAmeaca === 'Alto' ? 'selected' : '' %>>Alto</option>
                            <option value="Crítico" <%= missao.NivelAmeaca === 'Crítico' ? 'selected' : '' %>>Crítico</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">Localização</label>
                        <input type="text" name="local" class="form-control" value="<%= missao.Localizacao %>" required>
                    </div>
                    <div class="col-12 text-end">
                        <button type="submit" class="btn btn-success px-4">Salvar Alterações</button>
                    </div>
                </form>
            </div>
        </div>

        <a href="/missoes" class="btn btn-outline-secondary">Cancelar</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
