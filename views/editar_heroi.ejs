<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Herói - AEGIS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 shadow">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="fas fa-shield-halved"></i> AEGIS</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/herois">Heróis</a></li>
                    <li class="nav-item"><a class="nav-link" href="/viloes">Vilões</a></li>
                    <li class="nav-item"><a class="nav-link" href="/poderes">Poderes</a></li>
                    <li class="nav-item"><a class="nav-link" href="/missoes">Missões</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container py-5">
        <% if (success) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>
        <% if (error) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>
        <h2 class="mb-4"><i class="fas fa-edit"></i> Editar Herói</h2>

        <div class="card shadow-sm">
            <div class="card-body">
                <form action="/herois/editar/<%= heroi.IdHeroi %>" method="POST" class="row g-3">
                    <div class="col-md-3">
                        <label class="form-label">Nome Real</label>
                        <input type="text" name="nome" class="form-control" value="<%= heroi.Nome %>" required>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">Codinome</label>
                        <input type="text" name="codinome" class="form-control" value="<%= heroi.Codinome %>" required>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label">Identidade Secreta</label>
                        <input type="text" name="identidade" class="form-control" value="<%= heroi.IdentidadeSecreta %>">
                    </div>
                    <div class="col-md-2">
                        <label class="form-label">Status</label>
                        <select name="status" class="form-select">
                            <option value="Ativo" <%= heroi.StatusAtividade === 'Ativo' ? 'selected' : '' %>>Ativo</option>
                            <option value="Inativo" <%= heroi.StatusAtividade === 'Inativo' ? 'selected' : '' %>>Inativo</option>
                        </select>
                    </div>
                    <div class="col-md-1 d-flex align-items-end">
                        <button type="submit" class="btn btn-primary w-100">Salvar</button>
                    </div>
                </form>
            </div>
        </div>

        <div class="mt-3">
            <a href="/herois" class="btn btn-outline-secondary">Cancelar</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
